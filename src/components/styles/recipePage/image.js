import { makeStyles } from '@material-ui/core/styles';

export const useStylesImage = makeStyles({
  
  image: {
    maxWidth: '100%',
    height: 300,
    boxShadow: "none",
    borderRadius: 20,
  },
})