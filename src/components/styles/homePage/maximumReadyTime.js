import { makeStyles } from '@material-ui/core/styles';

export const useStylesMaximumReadyTime = makeStyles((theme) => ({
  root: {
    width: 300,
  },
  margin: {
    height: theme.spacing(3),
  },
}))