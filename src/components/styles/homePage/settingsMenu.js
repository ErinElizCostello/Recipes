import { makeStyles } from '@material-ui/core/styles';

export const useStylesSettingsMenu = makeStyles({
  
  settingsList: {
    width: 250,
  },

  text: {
    textDecoration: 'none', 
    color: 'inherit'
  }
})