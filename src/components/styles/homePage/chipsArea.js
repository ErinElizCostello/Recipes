import { makeStyles } from '@material-ui/core/styles';

export const useStylesChipsArea = makeStyles({
  display: {
    // width: 500,
    width: 325, 
    // display: 'flex', 
    // direction: 'row', 
    // flexWrap: 'wrap'
  },

  chipMargin: {
    margin: 2
  }
})