import { makeStyles } from '@material-ui/core/styles';

export const useStylesNoResults = makeStyles({
general: {
  display: 'flex',
  direction: 'row',
  justifyContent: 'center',
  alignItems: 'center',
  color: '#2e2e2e',
}
})